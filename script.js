document.addEventListener('DOMContentLoaded', () => {
    console.log('Página cargada correctamente.');

    // Example of a simple interaction if needed in the future:
    // const inviteButton = document.querySelector('.invite-friends');
    // if (inviteButton) {
    //     inviteButton.addEventListener('click', () => {
    //         alert('¡Gracias por invitar a tus amigos y familiares!');
    //     });
    // }

    // You could add dynamic sizing, animations, or more complex effects here
    // For instance, a subtle parallax effect on the background image based on scroll
    // (though the current design is mostly static, this would be a future enhancement).
});
